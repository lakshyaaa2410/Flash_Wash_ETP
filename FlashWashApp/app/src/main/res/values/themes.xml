<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.LocateMyAdvocate" parent="Theme.Material3.Light">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.LocateMyAdvocate" parent="Base.Theme.LocateMyAdvocate" />

    <style name="EditTextTheme" parent="Widget.AppCompat.EditText">
        <item name="android:textColorHint">#999999</item>  <!-- Hint text color -->
        <item name="android:textSize">16sp</item>  <!-- Hint text size -->
    </style>
</resources>